

# Tornar /jurisprudencia pública

Remover o `<ProtectedRoute>` wrapper da rota `/jurisprudencia` em `src/App.tsx`, permitindo acesso sem login.

**Arquivo:** `src/App.tsx`
- Linha da rota `/jurisprudencia`: trocar de `<ProtectedRoute><Jurisprudencia /></ProtectedRoute>` para `<Jurisprudencia />`

